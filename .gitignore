__pycache__/
*.py[cod]
.pytest_cache/
.venv/
.env
.DS_Store

# Runtime state, wherever it lands. These used to be anchored to data/ only, so
# the copies the app actually writes — backend/data/, or any directory it is
# launched from — were tracked and pushed. Match by name, not by location.
*.db
*.db-wal
*.db-shm
*.audit.key

# Coverage data (the human-readable report in reports/ is kept on purpose).
.coverage
.coverage.*
htmlcov/

# The optional neural voice model is a ~160MB download, never committed.
*.onnx
data/tts/

reports/*_console.txt
reports/*_time.txt

# 截图：可再生产物，不进仓库。
#
# 实测过一次：git ls-files 覆盖 195.9 MB，其中 185.1 MB（94%）是 PNG，而 173.5 MB
# 是 frontend_audit/screenshots/ 里 384 张**前端重构之前**那个界面的照片。它们既让
# 每次 clone 多拉两百兆，也让评委解开交付包时看到一个已经不存在的 UI。
#
# 磁盘上的文件一张都没删，shoot_pages.py 随时能重新生成：
#   python backend/scripts/shoot_pages.py http://127.0.0.1:8041 shots
# make_release.py 的 REGENERABLE_DIRS 已经把同样两个目录挡在交付包外面。
#
# 这一条原先写的是 `shots/` 加 `frontend_audit/screenshots/`，漏了两次，
# 漏法还不一样，值得都记下来：
#
# 一、**路径锚定**。`frontend_audit/screenshots/` 只挡那一个位置，所以新开的
#     frontend_redesign/final-polish/screenshots/（506 张、173.0 MB）整个漏进了
#     待提交清单。这正是上面第 10 行「Match by name, not by location」批评过的
#     写法，同一个错误在同一个文件里犯了第二次。
#
# 二、**名字每批都换**。`shots/` 是精确目录名，而实际拍出来的六批叫
#     shots_r22 / r22b / r22c / r22d / r22e / r22f，每批 252 张，合计 667.3 MB
#     ——比历史上那次 173.5 MB 大四倍，而且六批拍的是同一轮 R22 的同一套页面。
#     精确名字挡不住带后缀的名字，下一轮叫 shots_r23 又会漏。
#
# 所以两条都放宽成前缀 / 任意层级匹配。查过：git ls-files 在这些目录下命中 0 个
# 文件，仓库里 8 个已跟踪的 PNG 全是 backend/static/icons 和 harmonyos 的应用图标
# ——那些是产品资源，不受这两条影响。也查过没有任何测试或脚本引用 shots_r22*。
shots*/
screenshots/

# 第三方 skill 包，不是本项目的源码。
#
# 量过：.claude/skills/ 是 8.2 MB / 1077 文件，.agents/ 是 4.7 MB / 474 文件，
# 其中含一整个 node_modules。它们是 21 个外部 skill 包（impeccable、vercel-*、
# apple-design 等），各自带着自己的许可证，且用 SKILL_REGISTRY.md 里记着的安装
# 命令随时可以重新装齐——把它们提进一个**公开**的比赛仓库，既是十三兆的第三方
# 代码，也让人分不清哪些是我们写的。
#
# 项目自己写的那个 skill（youhuo-ui-constraints）已被跟踪，用 ! 放回来。
.claude/skills/*
!.claude/skills/youhuo-ui-constraints/
.agents/
node_modules/