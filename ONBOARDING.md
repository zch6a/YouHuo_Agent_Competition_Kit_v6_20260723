# 优活（YouHuo）后端交接说明

给刚接手这个仓库的人。目标读者是能读代码的人，所以这里不解释 FastAPI 怎么用，
只讲**这个仓库特有的**东西：它在哪、怎么跑、哪些约束不能破、以及前面的人在哪些
地方栽过跟头。

---

## 一、位置

| 东西 | 路径 |
|---|---|
| **工作目录（唯一的成熟版本）** | `F:\优活\YouHuo_Agent_Competition_Kit_v6_20260723` |
| Python 虚拟环境 | `F:\优活\YouHuo_Agent_Competition_Kit_v6_20260723\.venv` |
| 后端源码 | `backend/youhuo/` |
| 测试 | `backend/tests/`（约 1780 条） |
| 语音模型（**不在仓库里**） | `D:\youhuo-tts\`（`matcha-icefall-zh-baker` 139MB、`kokoro-multi-lang-v1_1` 407MB） |
| 重型验证报告 | `reports/` |

**没有第二个版本。** 桌面上、Downloads 里可能有旧的前端包，那些是历史素材，
不要往回合。以 `F:\优活\...v6_20260723` 为准。

**盘符纪律：** C: 盘不装任何东西。F: 盘曾经被写满到 0 字节，所以**新的工具链、
构建产物、模型文件一律放 D:**。仓库本身留在 F:。

---

## 二、十分钟跑起来

所有命令都在仓库根目录执行。

```bash
.venv/Scripts/python.exe -m uvicorn youhuo.api:app --app-dir backend --port 8000
```

跑测试（在 `backend/` 目录下）：

```bash
cd backend && ../.venv/Scripts/python.exe -m pytest -q
```

推送前必跑的密钥扫描：

```bash
.venv/Scripts/python.exe backend/scripts/scan_secrets.py
```

重型验证（五个阶段，约 4 分钟；改了 `backend/youhuo/` 之后必须重跑，
否则 `test_release_hygiene.py` 会红，因为报告里记着源码摘要）：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\verify_heavy.ps1
```

### 一个非跑不可的环境变量

演示数据默认**不种**。不设它的话，`/reminders` 回 0 条、`/bills` 空、
用药计划一份都没有——然后你会花半天去查一个不存在的缺陷。

```
YOUHUO_DEMO_STATE=normal
```

代码里等价的写法是 `create_app(..., seed_baseline_history=True)`。
测试夹具几乎都要带上它。

### 本机代理会坑你

这台机器上 `HTTP_PROXY=127.0.0.1:7897`，而 `urllib` 不认 `NO_PROXY`。
写「等服务起来」的轮询脚本时会卡住，然后你以为是服务没起来。
用 `urllib.request.ProxyHandler({})` 显式绕过。

---

## 三、分层：这是理解这个仓库的关键

后端**一共 139 个端点**，分成两类：

```
/v2 … /v7     102 个    产品的能力子系统，按版本迭代堆起来的
/api/v1        37 个    老人端 App 的门面（facade）
```

`/api/v1` **不是新的一套业务**，它是给老人端那一屏用的**翻译层**：
把 v2–v7 的能力包装成「一屏要什么就给什么」的形状，中文文案、
状态翻成人话、隐藏内部枚举。业务逻辑、状态机、审计链都在下面那 102 个里。

各层大致管什么：

| 层 | 数量 | 内容 |
|---|---|---|
| `/v2` | 14 | 对话引擎、任务、家属审批、提醒、审计、鉴权 |
| `/v3` | 9 | 同意记忆、文档分析、工具注册表、计划、委托 |
| `/v4` | 39 | 用药、作息例程、安全（SOS/心跳/无活动/策略）、联系人、健康事件、位置、情绪、报告、设备 |
| `/v5` | 21 | Saga、破玻璃、隐私导出与擦除、凭证校验、同步、语音消歧 |
| `/v6` | 15 | 适老交互档案、理解度、语音、交互计划、依赖度卡、玻璃盒、用户实验 |
| `/v7` | 4 | 个性化基线、关怀问答、日报、环境采样 |

### 加功能之前，先查下面有没有

**这是这个仓库最容易犯、代价最大的错误。** 已经犯过两次：

1. 加 `/api/v1/settings` 时，在 `memory_items` 里另存了一份字号和语速——
   而事实源是 `interaction_profiles_v6` 的 `font_scale` / `speech_rate`。
   后果：老人说「说慢一点」，它回答「好，我说慢一点」，档案 0.88 降到 0.80，
   **而合成语音仍然用 1.0 念**。
2. `/api/v1/emergency/call` 自己写了一套 SOS，完全不读 `safety_policies_v4`，
   于是 `notify_community` 这个开关从来没被读过，社区网格员永远不在接力名单里。

两次的共同点：**两边各自的测试都在自己的存储里往返（PUT 再 GET），所以都绿。**
1737 条测试没有一条能红。

所以：**新的老人端入口，判据必须跨子系统写。** 单端点往返一定绿，证明不了任何事。

---

## 四、硬约束（这些不能破）

### 安全

- 远端 `github.com/zch6a/YouHuo_Agent_Competition_Kit_v6_20260723` 是**公开**的，
  而且**有过一次审计密钥被推上公开仓库的事故**。
  → 每次推送前跑 `scan_secrets.py`，**红着不许提交**。
  → 不要因为扫描器挡了你的假 token 就去放宽扫描器的规则。
- 推送是对外动作，**必须由人明确同意**，不要自己推。
  目前 **20 个提交在本地，一个都没推。**

### 工程

- **禁止伪造 PASS。** 改不掉的写进 `KNOWN_ISSUES.md`，不要让它绿。
- **不声称零缺陷。** 说清测了什么、没测什么。
- **不得静默删除。** 控件、端点、字段要挪不要删；例程用「暂停」不用「删」。
- **P0 契约：渲染一张凭证，绝不允许创建/推进/批准/执行/重试/改动任何业务事务。**
  读就是读。

### 中文与编码

- **绝不用 PowerShell 读写含中文的源文件**（CP936 双重编码，会写出乱码）。
  用 Python + `io.open(..., encoding="utf-8")`。这条我今天又栽了一次。
- `.ps1` 含非 ASCII **必须**带 UTF-8 BOM；`.sh` **不能**带。
- 界面文案用「」，不用 ASCII 引号或弯引号。

### 界面（即使你只写后端也要知道，因为文案从后端出）

- **界面上不许出现英文枚举值。** `scheduled` 要翻成「待进行」。
- **不用 emoji 当图标。**
- 触控目标 ≥48px，主要操作 ≥56px。
- 全局 CSS 的四层结构由主 Claude 独占，不要并行改。

---

## 五、当前状态（截至 2026-08-17）

- `pytest`：**1780 passed / 0 failed**（最后一次全量；本文写作时正在跑一次
  时区修复后的回归，结果未出）
- 重型验证：五阶段全过
- `scan_secrets`：通过
- 仓库里没有模型文件（`check_artifacts_v6` 会把大文件当泄漏）
- `/api/v1`：37 个端点，36 个有类型化响应契约（`backend/youhuo/app_schemas.py`）

### 这一轮刚做完的

- **用药**（6 个端点）：今天吃什么/吃了没/还能吃几天、记「吃了」、记「没吃」、
  待确认列表、同意、拒绝。只读和记录，**不建计划不改剂量**——
  `create_medication_plan` 对 ELDER 角色建的计划直接生效，
  接到老人端等于老人可以自己给自己开药。
- **紧急呼叫改走安全策略**：按 `safety_policies_v4` 取接力名单，社区网格员进名单。
  措辞上**不说「已经联系了社区」**——这个原型不自动拨号。
- **固定安排（循环例程）**（4 个端点）：`recurring_routines` 建好了从没被用过，
  两张表都是空的。老人端此前只能建一次性提醒。

### 还没做的（v4/v5/v7 那侧都完整，缺的是老人端入口）

| 能力 | 端点 |
|---|---|
| 同意记忆 | `/v3/memories/propose\|decide\|revoke` —— 产品招牌，老人端只借用了那张表存设置 |
| 隐私导出与删除 | `/v5/privacy/export`、`/erase` |
| 情绪 | `/v4/emotions/analyze`、`/v4/reports/emotion` |
| 每日报告与基线 | `/v7/daily-report/{elder_id}`、`/v7/baseline` |

**明确不该上老人端的**（评委/研究/基础设施/家属侧）：
`/v3/tools/*`、`/v5/capability-truth|metrics|traces`、`/v6/studies/*`、
`/v6/competition/evidence`、`/v5/sagas|sync`、`/v4/contacts/faces/*`。

### 两个待人决定的事

- **前端可能整套作废。** 用户说过 `/app` 这套山水版前端可能不要了。
  所以后端必须能独立成立，判据不要依赖任何页面、选择器、DOM。
  `/app` 是第三套 consumer shell（另外两套是 `/elder` 和家属端），
  要不要退役 `/elder` 是用户的决定，已记在 `KNOWN_ISSUES.md` 的 P1-A。
- **`backend/static/app/index.html`** 是交接包的开发索引页，标题写着
  「优活老人端前端参考包」，链到「Claude 交接说明」，**公开可达**。推送前处理。

---

## 六、前面栽过的跟头（照着躲）

### 关于测试

1. **绿不等于测到了。** 每加一道判据，用变异证明它会红：把修复退回去，
   看那条判据红不红。今天 16 个变体里有 3 个「没咬到」，查下来两次是变体
   改错了地方、一次是被改的那行**根本不承重**（一句 `sorted()` 是死代码，
   顺序由 SQL 的 `ORDER BY` 提供）。变体没咬到时先问「这个变体真的改变行为了吗」，
   别急着放宽判据。

2. **跳过的测试等于没测。** 一条 `pytest.skip` 挂了很久没人看。

3. **别写按钟点随机红的判据。** 种子把服药记录铺到 `at_local(day, 8, jitter)` 为止，
   所以种子那份降压药**当地 8 点前是待服用、8 点后是已服用**。
   依赖它的判据早上红、下午绿——比赛当天红一次，然后所有人去查一个不存在的缺陷。
   要测空态就用 `seed_baseline_history=False`（那时一份用药计划都没有）。

4. **访客沙箱不是空的。** `/v2/auth/visitor` 会种自己的健康事件和用药计划。
   `verify_features_v6.py:162` 已经记过这个坑。

5. **阳性对照。** 断言「名单里没有社区」之前，先证明这个家庭**有**社区联系人；
   否则判据是靠「本来就没有」通过的。

### 关于并行

6. **做「快照→改→还原」的脚本只能前台跑。** 放后台跑、同时继续编辑同一个文件，
   还原那一步会把你的编辑写没，而且**两边都不报错**。今天发生过一次。
   `git diff --stat` 看不出来——行数对得上，内容是错的。
   要并行就先写个按内容核对的脚本（一组「变异残留」标记 + 一组「应该在」标记）。

7. **多个 agent 整文件覆写会互相抹掉。** 给每个 agent 独占的文件所有权。

### 关于「看起来对」

8. **驱动它，别读它。** 一次 109/109 的功能审计通过了，而 20 句自然话里
   15 句落到兜底回复。

9. **不要猜字段名。** 今天 `materialize_routines` 返回的键是 `occurrences_created`，
   我猜成 `created`，于是它**排了 8 条提醒却回「最近一周还没有要排的」**。
   不报错，只是说了句假话。查一下，别猜。

10. **时区。** 这一层曾经把老人说的「八点」直接存成 08:00 UTC，显示时再直接
    `strftime` 回去——层内自洽（说八点看到八点），但真实时刻是东八区下午四点。
    跨出这一层立刻暴露：循环例程存的是真实时刻，「每天早上八点」显示成 00:00。
    要读**墙上时间**的地方一律先过 `utils.local_now()`。

11. **偶发的红别当噪音。** 一次偶发红拉出了入口页唯一两个控件被动画藏了 1.3 秒。

---

## 七、写代码时的几条本地习惯

- 新端点要有 `App*` 响应模型（`app_schemas.py`，全是 `extra="forbid"` 的
  `StrictModel`）。此前 25 个端点全返回 `dict[str, Any]`，
  生成出来的 OpenAPI 是 25 个「有名字、零字段」的 object。
- 判据钉**守卫**，不钉 200。一个一路 200 的支付流程恰恰是最危险的那种。
- 加一个审计事件类型，**必须同时**在 `app_api.py` 的 `_WORDS` 里加一条中文，
  否则记录页落到兜底文案「办了一件事」，而那一行看起来完全正常。
  （现在有一条判据在查这件事了。）
- SQL 留在 store 里，不要写进 `app_api.py`。
- 错误要说人话：`raise HTTPException(400, "这个时间看不懂，请再说一遍。")`，
  不是把 Pydantic 的报错原样吐出去。

---

## 八、比赛背景

2026 中国高校计算机大赛 · 鸿蒙高校创新赛。产品是**面向老人的语音生活助手**，
核心主张是「同意记忆 + 可核验的代办」：它替老人办事（缴费、挂号、提醒），
每一步都留证据链，高风险动作要老人复述确认、要家人点头。

所以这个仓库里凡是涉及**说了什么**的地方都要克制：
不能把「查不到记录」说成「您没吃药」，不能把「社区在名单上」说成「已经通知社区」。
在养老场景里，一句说过头的话比少一个功能糟得多。
